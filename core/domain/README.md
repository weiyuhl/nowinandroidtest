# `:core:domain`

`core:domain` is retained as the shared UseCase module. There are currently no active UseCases after removing the old content business flows.

New UseCases should only be added here when they compose reusable business logic for multiple callers.
